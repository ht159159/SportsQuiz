@for $num from 1 through 6 {
  h#{$num},
  .h#{$num} {
    margin: 0;
    line-height: setFontSize(2.3+0.4 * 6, $num);
    font-size: setFontSize(2.3+0.4 * 6, $num);
  }
}
div,
span,
p,
label,
input,
textarea {
  margin: 0;
  line-height: 0.23rem;
  font-size: 0.23rem;
}

